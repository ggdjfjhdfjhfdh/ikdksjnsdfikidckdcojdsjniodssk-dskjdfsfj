---
import Layout from '../layouts/Layout.astro';
import WebGLThreatMap from '../components/WebGLThreatMap.astro';
---

<Layout title="Mapa de Amenazas WebGL - SESEC">
  <main class="webgl-page">
    <div class="container">
      <header class="page-header">
        <h1>Mapa de Amenazas en Tiempo Real</h1>
        <p class="subtitle">Visualización avanzada con WebGL - MapLibre GL + deck.gl</p>
      </header>
      
      <section class="map-section">
        <WebGLThreatMap />
      </section>
      
      <section class="info-section">
        <div class="info-grid">
          <div class="info-card">
            <h3>🎯 Puntos de Amenaza</h3>
            <p>Visualización de miles de puntos de amenaza con colores basados en severidad</p>
          </div>
          <div class="info-card">
            <h3>🌐 Conexiones de Ataque</h3>
            <p>Arcos animados que muestran las conexiones origen-destino de los ataques</p>
          </div>
          <div class="info-card">
            <h3>🔥 Mapa de Calor</h3>
            <p>Densidad de amenazas por región geográfica</p>
          </div>
          <div class="info-card">
            <h3>⚡ Rendimiento WebGL</h3>
            <p>Optimizado para manejar decenas de miles de puntos con FPS altos</p>
          </div>
        </div>
      </section>
    </div>
  </main>
</Layout>

<style>
  .webgl-page {
    min-height: 100vh;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
    padding: 2rem 0;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-header h1 {
    font-size: 3rem;
    font-weight: 700;
    background: linear-gradient(135deg, #00ffff, #0066ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: #888;
    margin: 0;
  }

  .map-section {
    margin-bottom: 4rem;
  }

  .info-section {
    margin-top: 4rem;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .info-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(0, 255, 255, 0.2);
    border-radius: 12px;
    padding: 2rem;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }

  .info-card:hover {
    transform: translateY(-5px);
    border-color: rgba(0, 255, 255, 0.5);
    box-shadow: 0 10px 30px rgba(0, 255, 255, 0.1);
  }

  .info-card h3 {
    color: #00ffff;
    font-size: 1.3rem;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .info-card p {
    color: #ccc;
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .page-header h1 {
      font-size: 2rem;
    }

    .container {
      padding: 0 1rem;
    }

    .info-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .info-card {
      padding: 1.5rem;
    }
  }
</style>

<!-- Import MapLibre GL CSS -->
<link href="https://unpkg.com/maplibre-gl@latest/dist/maplibre-gl.css" rel="stylesheet" />

<script>
  // Add any additional page-specific JavaScript here
  console.log('WebGL Threat Map page loaded');
</script>